/** Caller wallet debit per paid text message (after receiver's first reply). */
export const CHAT_CALLER_CHARGE_INR = 1;

/** Receiver wallet credit per paid text message (shown in receiver earnings). */
export const CHAT_RECEIVER_EARN_INR = 0.5;

export const CHAT_CALLER_FEE_LABEL = '₹1';
export const CHAT_RECEIVER_EARN_LABEL = '₹0.50';
